# I18N.md — Çoxdilli Dəstək (AZ / EN / RU)

> **Layihə:** ShopFlow E-Commerce Platform  
> **GitHub:** [your-username/shopflow](https://github.com/your-username/shopflow)  
> **Son yenilənmə:** 2026

---

## 1. Ümumi Baxış

| Parametr | Dəyər |
|---|---|
| Kitabxana | `next-intl` 3+ |
| Default dil | Azərbaycan (`az`) |
| Dəstəklənən dillər | AZ, EN, RU |
| URL strukturu | `/az/...` · `/en/...` · `/ru/...` |
| SEO | `hreflang` · `canonical` · ayrı `sitemap.xml` |
| Tarix/Valyuta | `Intl.DateTimeFormat` + `Intl.NumberFormat` |

**LMS-dən fərq:** `react-i18next` əvəzinə `next-intl` — SEO-friendly URL prefiksi, Server Component dəstəyi, `hreflang` avtomatik.

---

## 2. Quraşdırma

```bash
cd client
npm install next-intl next-sitemap
```

---

## 3. Konfiqurasiya

### 3.1 `next.config.ts`

```typescript
import type { NextConfig }    from 'next';
import createNextIntlPlugin    from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'res.cloudinary.com' },
      { protocol: 'https', hostname: 'lh3.googleusercontent.com' },
    ],
  },
};

export default withNextIntl(nextConfig);
```

### 3.2 `src/i18n/request.ts`

```typescript
import { getRequestConfig } from 'next-intl/server';
import { notFound }         from 'next/navigation';

const locales = ['az', 'en', 'ru'] as const;
export type Locale = (typeof locales)[number];

export default getRequestConfig(async ({ locale }) => {
  if (!locales.includes(locale as Locale)) notFound();
  return {
    messages: (await import(`../i18n/${locale}.json`)).default,
  };
});
```

### 3.3 `middleware.ts` (root qovluqda)

```typescript
import createMiddleware from 'next-intl/middleware';

export default createMiddleware({
  locales:         ['az', 'en', 'ru'],
  defaultLocale:   'az',
  localeDetection: true,
});

export const config = {
  matcher: ['/((?!api|_next|_vercel|.*\\..*).*)'],
};
```

### 3.4 Root Layout

```typescript
// src/app/layout.tsx
import { NextIntlClientProvider, useMessages } from 'next-intl';
import { Inter }                               from 'next/font/google';

const inter = Inter({ subsets: ['latin', 'cyrillic'] });

export default function RootLayout({
  children,
  params: { locale },
}: {
  children: React.ReactNode;
  params:   { locale: string };
}) {
  const messages = useMessages();

  return (
    <html lang={locale} dir="ltr">
      <body className={inter.className}>
        <NextIntlClientProvider locale={locale} messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

---

## 4. Metadata — SEO hreflang

### 4.1 Root Metadata

```typescript
// src/app/[locale]/layout.tsx
import { getTranslations } from 'next-intl/server';
import type { Metadata }   from 'next';

const siteUrl = 'https://shopflow.az';

export async function generateMetadata({
  params: { locale },
}: { params: { locale: string } }): Promise<Metadata> {
  const t = await getTranslations({ locale, namespace: 'meta' });

  return {
    metadataBase: new URL(siteUrl),
    title: {
      default:  t('site_title'),
      template: `%s | ${t('site_name')}`,
    },
    description: t('site_description'),
    alternates: {
      canonical: `${siteUrl}/${locale}`,
      languages: {
        'az':        `${siteUrl}/az`,
        'en':        `${siteUrl}/en`,
        'ru':        `${siteUrl}/ru`,
        'x-default': `${siteUrl}/az`,
      },
    },
    openGraph: {
      siteName: t('site_name'),
      locale:   locale === 'az' ? 'az_AZ'
              : locale === 'en' ? 'en_US' : 'ru_RU',
    },
  };
}
```

### 4.2 Dinamik Məhsul Metadata

```typescript
// src/app/[locale]/(shop)/products/[slug]/page.tsx
export async function generateMetadata({
  params: { locale, slug },
}: { params: { locale: string; slug: string } }): Promise<Metadata> {
  const product = await getProductBySlug(slug);
  const t       = await getTranslations({ locale, namespace: 'product' });

  if (!product) return { title: t('not_found') };

  return {
    title:       product.name,
    description: product.description.slice(0, 160),
    keywords:    product.tags.join(', '),
    openGraph: {
      title:       product.name,
      description: product.description.slice(0, 160),
      images: [{ url: product.images[0]?.url, width: 1200, height: 630 }],
    },
    twitter: {
      card:        'summary_large_image',
      title:       product.name,
      description: product.description.slice(0, 160),
      images:      [product.images[0]?.url],
    },
    alternates: {
      canonical: `${siteUrl}/${locale}/products/${slug}`,
      languages: {
        'az':        `${siteUrl}/az/products/${slug}`,
        'en':        `${siteUrl}/en/products/${slug}`,
        'ru':        `${siteUrl}/ru/products/${slug}`,
        'x-default': `${siteUrl}/az/products/${slug}`,
      },
    },
  };
}
```

---

## 5. Komponentlərdə İstifadə

```typescript
// Server Component
import { getTranslations } from 'next-intl/server';

export default async function HomePage({ params: { locale } }: { params: { locale: string } }) {
  const t = await getTranslations({ locale, namespace: 'home' });
  return <h1>{t('hero_title')}</h1>;
}

// Client Component
'use client';
import { useTranslations } from 'next-intl';

export default function ProductCard({ product }: { product: Product }) {
  const t = useTranslations('product');
  return (
    <div>
      <Button>{t('add_to_cart')}</Button>
      {product.stock === 0 && <Badge>{t('out_of_stock')}</Badge>}
    </div>
  );
}

// Dəyişən ilə
t('greeting', { name: user.name });
// az.json: "greeting": "Salam, {name}!"

// Plural
t('items_count', { count: 3 });
// az.json: "items_count": "{count, plural, one {# məhsul} other {# məhsul}}"
```

---

## 6. LanguageSwitcher Komponenti

```typescript
// src/components/ui/LanguageSwitcher.tsx
'use client';

import { useLocale }              from 'next-intl';
import { useRouter, usePathname } from 'next-intl/client';
import { Globe }                  from 'lucide-react';
import { useState }               from 'react';

const languages = [
  { code: 'az', label: 'Azərbaycanca', flag: '🇦🇿' },
  { code: 'en', label: 'English',      flag: '🇬🇧' },
  { code: 'ru', label: 'Русский',      flag: '🇷🇺' },
] as const;

export function LanguageSwitcher() {
  const locale      = useLocale();
  const router      = useRouter();
  const pathname    = usePathname();
  const [open, setOpen] = useState(false);
  const current     = languages.find((l) => l.code === locale);

  return (
    <div className="relative">
      <button
        onClick={() => setOpen(!open)}
        className="flex items-center gap-2 px-3 py-2 rounded-lg hover:bg-gray-100 text-sm"
      >
        <Globe size={16} />
        <span>{current?.flag} {current?.label}</span>
      </button>

      {open && (
        <div className="absolute right-0 top-full mt-1 bg-white border
                        border-gray-200 rounded-lg shadow-lg py-1 z-50 min-w-[160px]">
          {languages.map((lang) => (
            <button
              key={lang.code}
              onClick={() => { router.replace(pathname, { locale: lang.code }); setOpen(false); }}
              className={`w-full flex items-center gap-2 px-4 py-2 text-sm hover:bg-gray-50
                          ${locale === lang.code ? 'font-medium text-accent' : 'text-gray-700'}`}
            >
              <span>{lang.flag}</span>
              <span>{lang.label}</span>
              {locale === lang.code && <span className="ml-auto">✓</span>}
            </button>
          ))}
        </div>
      )}
    </div>
  );
}
```

---

## 7. Sitemap (SEO)

### `next-sitemap.config.js`

```javascript
/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl:           'https://shopflow.az',
  generateRobotsTxt: true,
  sitemapSize:       5000,
  exclude: [
    '/admin', '/admin/*', '/vendor', '/vendor/*',
    '/checkout', '/cart', '/profile', '/profile/*',
    '/wishlist', '/orders', '/orders/*',
    '/login', '/register', '/reset-password',
  ],
  robotsTxtOptions: {
    policies: [
      { userAgent: '*', allow: '/' },
      { userAgent: '*', disallow: ['/admin', '/vendor', '/checkout', '/cart'] },
    ],
    additionalSitemaps: ['https://shopflow.az/server-sitemap.xml'],
  },
  transform: async (config, path) => ({
    loc:        path,
    changefreq: 'weekly',
    priority:   path === '/' ? 1.0 : 0.7,
    lastmod:    new Date().toISOString(),
    alternateRefs: [
      { href: `https://shopflow.az/az${path}`, hreflang: 'az' },
      { href: `https://shopflow.az/en${path}`, hreflang: 'en' },
      { href: `https://shopflow.az/ru${path}`, hreflang: 'ru' },
    ],
  }),
};
```

### Dinamik Sitemap (Məhsullar + Kateqoriyalar)

```typescript
// src/app/server-sitemap.xml/route.ts
import { getServerSideSitemap, ISitemapField } from 'next-sitemap';

const locales = ['az', 'en', 'ru'];
const siteUrl = 'https://shopflow.az';

export async function GET() {
  const [productsRes, categoriesRes] = await Promise.all([
    fetch(`${process.env.NEXT_PUBLIC_API_URL}/products?limit=1000&isActive=true`,
      { next: { revalidate: 3600 } }),
    fetch(`${process.env.NEXT_PUBLIC_API_URL}/categories`,
      { next: { revalidate: 86400 } }),
  ]);

  const { data: products }   = await productsRes.json();
  const { data: categories } = await categoriesRes.json();

  const fields: ISitemapField[] = [];

  for (const product of products) {
    for (const locale of locales) {
      fields.push({
        loc:        `${siteUrl}/${locale}/products/${product.slug}`,
        changefreq: 'daily',
        priority:   0.8,
        lastmod:    product.updatedAt,
        alternateRefs: locales.map((l) => ({
          href: `${siteUrl}/${l}/products/${product.slug}`, hreflang: l,
        })),
      });
    }
  }

  for (const category of categories) {
    for (const locale of locales) {
      fields.push({
        loc:        `${siteUrl}/${locale}/category/${category.slug}`,
        changefreq: 'weekly',
        priority:   0.7,
        alternateRefs: locales.map((l) => ({
          href: `${siteUrl}/${l}/category/${category.slug}`, hreflang: l,
        })),
      });
    }
  }

  return getServerSideSitemap(fields);
}
```

```json
// package.json — postbuild hook
{
  "scripts": {
    "build":     "next build",
    "postbuild": "next-sitemap"
  }
}
```

---

## 8. Tərcümə Faylları

### `src/i18n/az.json`

```json
{
  "meta": {
    "site_name":        "ShopFlow",
    "site_title":       "ShopFlow — Online Alış-veriş",
    "site_description": "Azərbaycanda ən geniş çeşid, ən sürətli çatdırılma"
  },
  "common": {
    "loading": "Yüklənir...", "error": "Xəta baş verdi", "retry": "Yenidən cəhd et",
    "save": "Yadda saxla", "cancel": "Ləğv et", "delete": "Sil", "edit": "Düzəlt",
    "add": "Əlavə et", "search": "Axtar...", "filter": "Filter",
    "clear_filters": "Filterləri Sıfırla", "sort": "Sırala",
    "back": "Geri", "next": "İrəli", "previous": "Geri",
    "confirm": "Təsdiq et", "close": "Bağla", "view_all": "Hamısına bax",
    "no_results": "Nəticə tapılmadı", "required_field": "Bu sahə məcburidir",
    "language": "Dil"
  },
  "auth": {
    "login": "Daxil ol", "register": "Qeydiyyat", "logout": "Çıxış",
    "email": "Email", "password": "Şifrə", "confirm_password": "Şifrəni təsdiqlə",
    "name": "Ad Soyad", "forgot_password": "Şifrəni unutdunuz?",
    "reset_password": "Şifrəni sıfırla", "google_login": "Google ilə daxil ol",
    "no_account": "Hesabınız yoxdur?", "have_account": "Hesabınız var?",
    "login_success": "Uğurla daxil oldunuz", "logout_success": "Çıxış uğurlu oldu",
    "register_success": "Qeydiyyat tamamlandı. Emailinizi yoxlayın.",
    "reset_link_sent": "Sıfırlama linki göndərildi",
    "password_reset_ok": "Şifrə uğurla yeniləndi"
  },
  "nav": {
    "home": "Ana Səhifə", "products": "Məhsullar", "categories": "Kateqoriyalar",
    "cart": "Səbət", "wishlist": "İstək Siyahısı", "orders": "Sifarişlərim",
    "profile": "Profil", "settings": "Parametrlər", "search": "Axtar"
  },
  "product": {
    "add_to_cart": "Səbətə əlavə et", "add_to_wishlist": "İstək siyahısına əlavə et",
    "buy_now": "İndi al", "out_of_stock": "Stokda yoxdur", "in_stock": "Stokda var",
    "low_stock": "Az qalıb: {count} ədəd", "discount": "{percent}% endirim",
    "featured": "Öne Çıxan", "not_found": "Məhsul tapılmadı",
    "added_to_cart": "Səbətə əlavə edildi",
    "sort_newest": "Ən yeni", "sort_price_asc": "Qiymət: Ucuzdan bahaya",
    "sort_price_desc": "Qiymət: Bahadan ucuza",
    "sort_rating": "Ən yüksək reytinq", "sort_popular": "Ən populyar",
    "showing": "{from}-{to} / {total} məhsul"
  },
  "cart": {
    "title": "Səbət", "empty": "Səbətiniz boşdur",
    "empty_desc": "Bəyəndiyiniz məhsulları səbətə əlavə edin",
    "shop_now": "Alış-verişə Başla",
    "subtotal": "Ara cəm", "shipping": "Çatdırılma",
    "free_shipping": "Pulsuz çatdırılma", "discount": "Endirim", "total": "Ümumi",
    "checkout": "Sifarişi Tamamla",
    "coupon_code": "Kupon kodu", "apply_coupon": "Tətbiq et",
    "coupon_applied": "{code} — {discount} endirim tətbiq edildi",
    "items_count": "{count, plural, one {# məhsul} other {# məhsul}}",
    "free_shipping_progress": "₼{remaining} daha əlavə edin, pulsuz çatdırılma qazanın!"
  },
  "order": {
    "title": "Sifarişlərim", "number": "Sifariş №{number}",
    "empty": "Hələ sifarişiniz yoxdur",
    "success_title": "Sifarişiniz qəbul edildi! 🎉",
    "success_message": "Sifariş №{number} uğurla yaradıldı.",
    "status_pending": "Gözləyir", "status_confirmed": "Təsdiqləndi",
    "status_processing": "Hazırlanır", "status_shipped": "Göndərildi",
    "status_delivered": "Çatdırıldı", "status_cancelled": "Ləğv edildi",
    "status_refunded": "Geri qaytarıldı"
  },
  "review": {
    "title": "Rəylər", "write": "Rəy yaz", "submit": "Göndər",
    "submitted": "Rəyiniz moderasiya üçün göndərildi",
    "verified": "Təsdiqlənmiş alqı",
    "no_reviews": "Hələ rəy yoxdur. İlk rəyi siz yazın!",
    "total_reviews": "{count} rəy"
  },
  "error": {
    "not_found": "Səhifə tapılmadı", "server_error": "Server xətası",
    "unauthorized": "Giriş icazəsi yoxdur",
    "forbidden": "Bu əməliyyat üçün icazəniz yoxdur",
    "back_home": "Ana Səhifəyə Qayıt", "try_again": "Yenidən Cəhd Et",
    "product_not_found": "Məhsul tapılmadı"
  }
}
```

### `src/i18n/en.json` (əsas açarlar)

```json
{
  "meta": {
    "site_name": "ShopFlow",
    "site_title": "ShopFlow — Online Shopping",
    "site_description": "The widest selection, fastest delivery in Azerbaijan"
  },
  "product": {
    "add_to_cart": "Add to Cart", "out_of_stock": "Out of Stock",
    "in_stock": "In Stock", "low_stock": "Only {count} left",
    "discount": "{percent}% off", "featured": "Featured",
    "added_to_cart": "Added to cart",
    "sort_newest": "Newest", "sort_price_asc": "Price: Low to High",
    "sort_price_desc": "Price: High to Low",
    "sort_rating": "Highest Rated", "sort_popular": "Most Popular",
    "showing": "{from}-{to} of {total} products"
  },
  "cart": {
    "title": "Cart", "empty": "Your cart is empty",
    "subtotal": "Subtotal", "shipping": "Shipping",
    "free_shipping": "Free shipping", "total": "Total",
    "checkout": "Checkout",
    "coupon_applied": "{code} — {discount} discount applied",
    "items_count": "{count, plural, one {# item} other {# items}}"
  },
  "order": {
    "status_pending": "Pending", "status_confirmed": "Confirmed",
    "status_processing": "Processing", "status_shipped": "Shipped",
    "status_delivered": "Delivered", "status_cancelled": "Cancelled",
    "status_refunded": "Refunded",
    "success_title": "Order Placed Successfully! 🎉"
  },
  "error": {
    "not_found": "Page Not Found", "server_error": "Server Error",
    "back_home": "Back to Home", "try_again": "Try Again"
  }
}
```

### `src/i18n/ru.json` (əsas açarlar)

```json
{
  "meta": {
    "site_name": "ShopFlow",
    "site_title": "ShopFlow — Интернет-магазин",
    "site_description": "Широкий выбор, быстрая доставка по Азербайджану"
  },
  "product": {
    "add_to_cart": "В корзину", "out_of_stock": "Нет в наличии",
    "in_stock": "В наличии", "low_stock": "Осталось {count} шт.",
    "discount": "Скидка {percent}%", "featured": "Рекомендуем",
    "added_to_cart": "Добавлено в корзину",
    "sort_newest": "Новинки", "sort_price_asc": "Цена: по возрастанию",
    "sort_price_desc": "Цена: по убыванию",
    "sort_rating": "По рейтингу", "sort_popular": "Популярные",
    "showing": "{from}-{to} из {total} товаров"
  },
  "cart": {
    "title": "Корзина", "empty": "Ваша корзина пуста",
    "subtotal": "Подитог", "shipping": "Доставка",
    "free_shipping": "Бесплатная доставка", "total": "Итого",
    "checkout": "Оформить заказ",
    "coupon_applied": "{code} — скидка {discount} применена",
    "items_count": "{count, plural, one {# товар} few {# товара} other {# товаров}}"
  },
  "order": {
    "status_pending": "Ожидает", "status_confirmed": "Подтверждён",
    "status_processing": "Обрабатывается", "status_shipped": "Отправлен",
    "status_delivered": "Доставлен", "status_cancelled": "Отменён",
    "status_refunded": "Возвращён",
    "success_title": "Заказ успешно оформлен! 🎉"
  },
  "error": {
    "not_found": "Страница не найдена", "server_error": "Ошибка сервера",
    "back_home": "На главную", "try_again": "Попробовать снова"
  }
}
```

---

## 9. Valyuta & Tarix Formatı

```typescript
// src/utils/formatPrice.ts
export const formatPrice = (amount: number, locale = 'az'): string => {
  const localeMap: Record<string, string> = {
    az: 'az-AZ', en: 'en-AZ', ru: 'ru-AZ',
  };
  return new Intl.NumberFormat(localeMap[locale] ?? 'az-AZ', {
    style: 'currency', currency: 'AZN',
    minimumFractionDigits: 2, maximumFractionDigits: 2,
  }).format(amount);
};
// → az: "₼2,499.99" | en: "AZN 2,499.99" | ru: "2 499,99 ₼"

// src/utils/formatDate.ts
export const formatDate = (date: string | Date, locale = 'az'): string => {
  const localeMap: Record<string, string> = {
    az: 'az-AZ', en: 'en-US', ru: 'ru-RU',
  };
  return new Intl.DateTimeFormat(localeMap[locale] ?? 'az-AZ', {
    year: 'numeric', month: 'long', day: 'numeric',
  }).format(new Date(date));
};
// → az: "28 yanvar 2026" | en: "January 28, 2026" | ru: "28 января 2026 г."
```

---

## 10. Yeni Açar Əlavə Etmə Qaydası

```bash
# 1. az.json — əsas dil (HƏR ZAMAN əvvəl)
# 2. en.json — ingilis
# 3. ru.json — rus
# 4. Komponentdə: const t = useTranslations('namespace'); t('key')
```

**Qayda:** Açar `az.json`-da yoxdursa `key is missing` xəbərdarlığı verir. Hər üç dildə əlavə et.

---

## 11. I18N Qaydaları

```
AÇAR ADLANDIRMA
  ✅  Namespace.key formatı      → 'product.add_to_cart'
  ✅  snake_case açarlar         → 'out_of_stock'
  ✅  Dəyişənlər {curly_braces}  → 'Salam, {name}!'
  ❌  3 səviyyədən dərin iç-içə

SEO
  ✅  generateMetadata() hər dinamik səhifədə
  ✅  hreflang bütün səhifələrdə (az/en/ru + x-default)
  ✅  Canonical URL dil prefiksi ilə
  ✅  Sitemap hər dil üçün URL-lər

URL
  ✅  /az/ /en/ /ru/ prefiksi
  ✅  Middleware bütün route-lara tətbiq edilir
  ✅  LanguageSwitcher cari path-ı saxlayır
  ✅  postbuild hook ilə sitemap avtomatik yaradılır
```
